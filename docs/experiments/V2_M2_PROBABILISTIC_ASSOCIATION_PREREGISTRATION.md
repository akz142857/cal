# V2-M2 概率多轨迹关联复审预注册

冻结日期：2026-07-24  
状态：实现前冻结，留出集尚未运行

## 假设

最近邻硬匹配在两个同外观刚性结构真正穿越时交换轨迹身份。新的候选只能改变
数据关联机制：用动作条件运动预测、外因速度、刚性关系和多个全局关联假设形成
后验；关联不确定时延迟身份承诺，分离后恢复原轨迹。

不能通过增加主体标签、读取模拟器身份、改变真值匹配容差或降低原 0.90 身份门
解决问题。

## 冻结分割

- 开发种子：600–615，可以反复运行；
- 留出种子：9101–9116，在代码、单元测试和门槛冻结后只运行一次；
- 场景由种子固定映射到四种穿越方向、四种持续时间、正负旋转、不同遮挡相位和
  三档视觉坐标噪声；
- 精确配置与门槛见
  [`V2_M2_PROBABILISTIC_ASSOCIATION_PROTOCOL.json`](../../experiments/V2_M2_PROBABILISTIC_ASSOCIATION_PROTOCOL.json)。

## 正式门

交叉身份保持均值必须不低于 0.90，最差场景族不低于 0.75，交换率不高于
0.10；同时节点 F1、刚性边、遮挡身份、关联 Brier、资源和标签隔离全部通过。
任何单项失败都保持 `stop_before_v2_m3`。

留出运行后不得调参并重跑同一留出；若失败，下一假设必须使用新的冻结留出。
