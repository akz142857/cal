# V2-M1–M3 综合确认报告

日期：2026-07-24

## 决策

**全新数据综合确认通过，确认当前 M1–M3 行为链，并授权设计无特权 V2-M4。**

正式确认只运行一次，使用 17001–17216 新种子。旧 M2/M3 留出结果读取列表和
旧留出种子使用列表均为空。v2 协议 SHA-256 为
`d9ca5a58009e6f92dcb14f46403b2f579fa50945f72d042aabc29565b958519b`。

## 协议审计

v1 开发结果完整保留。其唯一失败是对 0.5/0.5 平坦后验执行任意 MAP 平局后的
拓扑 F1 门。确认运行前发布的 v2 不修改 M1、M2、M3 算法，改用概率、Brier、
NLL 和不收敛直接检验无因果似然消融，并启用另一批全新确认种子。

一次性确认完成后发布的 v3 修订（SHA-256
`505156c835472828a6442cb966b92f82ae5b1462c0b8716f789deb684776214a`）只把评测器中硬编码为 True 的安全门
改为等价的运行时计算，不改变任何阈值、种子或阶段算法；v3 的修订记录
保留 v2 协议与一次性确认结果的哈希，本审计逐项验证该链条。开发集结果
在 v3 下重新生成并全部通过。

随后发布的 v4 修订（SHA-256 `c1d04f17e11691bfe019541d62175133105a3eaecd704d42d14a9ae82a08a8e2`）给
`OnlineEntityGraph` 加了一个默认值为 6（与此前硬编码值完全相同）的
`reacquisition_window` 构造参数，只让主动选择更长遮挡窗口的新调用方
（V2-I1 系统集成探针）受益；M1/M2/M3/本确认脚本均未传入该参数，行为
不变。这不是靠论证声称不变，而是逐字段重跑验证：v4 修改前后的 M2、
M3（含无因果似然消融）开发集产物，除 `cpu_wall_seconds`（计时，非
行为输出）外逐字段完全一致。v4 的修订记录同样保留 v3 协议与一次性
确认结果的哈希，本审计逐项验证该链条。开发集结果在 v4 下重新生成
并全部通过。

随后发布的 v5 修订（SHA-256 `43234417b1458a708639b3a3892f50c2ef789d83b8c7268f8fdb2f53c1784b96`）给
`OnlineEntityGraph` 加了一个默认值为 0.0（无操作）的
`identity_switch_penalty_weight` 构造参数，只让主动启用该惩罚项的新
调用方（V2-I1 系统集成探针，用于抑制自我与干扰物近距离交会时的身份
互换）受益；M1/M2/M3/本确认脚本均未传入该参数，行为不变。同样是逐
字段重跑验证而非论证：v5 修改前后的 M2（三种关联模式）、M3（含无
因果似然消融）开发集产物，除 `cpu_wall_seconds` 外逐字段完全一致。
v5 的修订记录同样保留 v4 协议与一次性确认结果的哈希，本审计逐项
验证该链条。开发集结果在 v5 下重新生成并全部通过。

随后发布的 v6 修订（SHA-256 `70120b65eeac539d1f5d978f47bdd15ee9136e297e1413c4fc3235c54e8f18f9`）给
`OnlineEntityGraph` 加了一个默认值为 `None`（无操作）的
`drift_reset_after` 构造参数，只让主动启用该恢复机制的新调用方
（V2-I1 系统集成探针）受益；M1/M2/M3/本确认脚本均未传入该参数，
行为不变。同样是逐字段重跑验证而非论证：v6 修改前后的 M2（三种
关联模式）、M3（含无因果似然消融）开发集产物，除 `cpu_wall_seconds`
外逐字段完全一致。v6 的修订记录同样保留 v5 协议与一次性确认结果的
哈希，本审计逐项验证该链条。开发集结果在 v6 下重新生成并全部通过。

## 确认结果

| 阶段 | 正式结果 | 机制对照 |
| --- | --- | --- |
| M1 | 平均 F1 0.9995；主动步数减少 51.3% | 删除动作下降 1.000；删除失败更新下降 1.000 |
| M2 | 交叉身份保持 0.9792；最差方向族 0.9167；Brier 0.0041 | 最近邻身份保持 0.6667 |
| M3 | 对称最大偏离 0.000000；破缺后真实图概率 0.999991；最慢 2 步 | 删除因果似然：概率 0.500、Brier 0.500、NLL 0.693、73 步不收敛 |

身份与结构结果：

- M2 正常节点 F1：0.9906；
- M2 刚性边 F1：0.9844；
- M2 交叉交换率：0.0208；
- M3 完整图拓扑 F1：1.000；
- M3 姿态身份保持：0.9963。

组合端点资源为 488 参数、
24,384 B 活动状态和
2,304 MAC/步。

## 解释边界

- 这是行为组合确认；历史 M1 状态不会直接序列化并传给 M2，M2 会重新学习控制关系。
- 输入仍是二值点图或解析稀疏视觉节点，不是原始像素前端。
- M3 只确认两个完整候选，并使用已知二连杆姿态渲染器。
- 本结果只授权设计无特权 V2-M4，不代表 M4 或对象永久性已经通过。

因此下一步可以设计无特权 M4，但应继续停在正式 M4 门之前：先从视觉自身推断
free/occupied/unknown，再冻结新的开发集和一次性确认集。
