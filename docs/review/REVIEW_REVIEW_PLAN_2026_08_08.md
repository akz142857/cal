# 评审报告：REVIEW_PLAN.md（评审方案自身）2026-08-08

范围：`docs/review/REVIEW_PLAN.md` v1 草案全文（约 530 行）。
方法：对方案中每一个可核查的事实断言做仓库级复核（命令 + 数字），再检查
内部交叉引用一致性与可实施性。这是把方案自己的标准用回它自己：
"跑出一个数"优先于"讲得有道理"。
判定：**pass_with_conditions**（P0=0，P1=1 已修复，P2=4 已修复，P3=3 已修复/记录）

---

## 事实核查台账

方案的可信度取决于它自己的断言是否经得起复核。逐条验证：

| # | 方案断言 | 核查命令 | 实测 | 结论 |
| --- | --- | --- | --- | --- |
| C1 | 边车"全部 31 份" | `ls experiments/*.sha256 \| wc -l` | 31 | ✅ |
| C2 | CI "9 个 matrix group" | `grep -c "group:" .github/workflows/tests.yml` | **8** | ❌ F3 |
| C3 | "约 10 个新模块" | `ls cal/model/stochastic_motion_filter.py cal/evaluation/*permanence* cal/evaluation/randomized_occlusion_world.py \| wc -l` | **14** | ❌ F5 |
| C4 | V8 留出 24 门中 3 门失败 | `RESEARCH_STATUS.md` | 21/24 通过、3 失败 | ✅ |
| C5 | V8 捷径根因（`x=10`、`row 12`、seed 奇偶） | 诊断草案 A.2 节引用的 `v2_i1_integration.py:38-63` | 一致 | ✅ |
| C6 | V5 留出因对照不可构造而中止且不可重试 | `README.md` | 一致 | ✅ |
| C7 | 当前生效协议为 V7 而非 CLAUDE.md 所称 V3 | R1 漂移脚本 + `protocol_version` 字段 | V7 无漂移，V3–V6 已被取代 | ✅ |
| C8 | R1/6.2 两段检查脚本可运行 | 实跑 | 输出与文档记载一致 | ✅ |
| C9 | 角色独立性可实施（"必须与作者不同人"） | `git log --format='%an <%ae>' \| sort -u` | **仓库仅一位人类作者**（ziy，两个历史邮箱经 `.mailmap` 合并） | ❌ F1 |

## 发现台账

| # | 级别 | 判定 | 描述 | 去向 |
| --- | --- | --- | --- | --- |
| F1 | **P1** | CONFIRMED | 方案多处要求"与作者不同人"（主评审、红队、裁决人、Gatekeeper），但仓库只有一位人类贡献者，**照字面执行则整个轨道 A 永远无法启动**。方案缺"角色如何实例化"一节。事实上 2026-07-26 复核已经给出可行先例：独立性由**不共享上下文的独立 agent 会话**提供，人类保留 Gatekeeper 与裁决人职责 | 已修复：新增 §2.4 角色实例化 [勘误¹] |
| F2 | P2 | CONFIRMED | §3.1 R5 写"按 §7 分级汇总发现"，但分级在 §8，§7 是文档一致性清单。错误交叉引用会把执行者引向错误章节 | 已修复 |
| F3 | P2 | CONFIRMED | §6.2 称 CI 有"9 个 matrix group"，实际 8 个。在一份把"数字必须可复核"立为原则的文档里，未复核的数字是自伤 | 已修复 |
| F4 | P2 | CONFIRMED | R0 末条列出**五**项要求（假设、固定指标、对照组、split 角色、停止规则）却写"**四**者缺一"。执行者无法判断哪四项是硬性的 | 已修复：改为"五者" |
| F5 | P3 | CONFIRMED | §0 称"约 10 个新模块"，实际 14 个（含 2 个 `_` 前缀基线模块与 `permanence_geometry_diagnostic.py`）。影响 §10 阶段 3 的工作量估计 | 已修复 |
| F6 | P2 | CONFIRMED | 附录 B 报告模板含"红队攻击台账""争议与裁决"两节，但 §9 交付物表没有对应行；模板与交付物清单在红队引入后未同步 | 已修复 |
| F7 | P3 | CONFIRMED | §6.2 对 CI 覆盖缺口的文字描述不完整：glob 覆盖 `[a-i]` 与 `[m-u]`，未覆盖的除 `j/k/l` 外还有 **`v`–`z` 首字母**（今天靠显式清单救着 `test_v2_*` 与 `test_world`，新增 `test_w*` 等会静默掉出）。脚本是对的，文字低估了缺口面 | 已修复 |
| F8 | P3 | PLAUSIBLE | 红队胜利条件定为"通过**全部**门"是对的（对照门存在的意义就是抓捷径，捷径被对照门抓住说明协议在工作），但方案没写**接近成功**怎么处置：作弊方案通过全部主门、仅被一个对照门拦下，与被全部对照门拦下，严重性完全不同 | 已修复：§2.2 增加近失分级 |

## 未上台账但记录在案

- 方案自身的修订规则缺失：它规定评审报告不可在位修改，却没规定方案自己
  改版怎么处理。已在头部加入修订记录约定（方案是流程文档不是证据，允许
  修订，但须留版本痕迹）。
- §2 角色表中对抗验证者的"适用轨道"写"A"而其他行写"仅 A"，口径不一，
  已统一。
- R1 的锁状态示例表内嵌了 `4e0c7c2` 时点的实测结果，会随仓库演进过时——
  可接受，因为表头已钉住 commit，且它的作用是教"如何归类"而非陈述现状。

## 结论

方案的骨架（不对称红队、举证责任在正方、相持判 block、两轨道分层）经复核
成立，且与项目已有实践（不知情验证者、V7 先例、V8 根因）对得上。被证伪的
是三处具体数字/引用和一个结构性缺口（F1）。所有 CONFIRMED 项已在
`REVIEW_PLAN.md` v1.1 中修复。

---

## 勘误

按 §9.1，本报告原则上不在位修改。以下更正经作者明确授权后就地施行，且**只限
不改变任何判定或结论的笔误**；一切实质性更正仍走补充文档。

**勘误¹（2026-08-08）**：F1 去向栏原写"新增 §2.5 角色实例化"，正确章节号为
**§2.4**——新增节占 §2.4，原"通用约束"顺延为 §2.5。已就地更正。

**未就地更正的一项**：F5 的去向栏写"已修复"，但实际只改了 `REVIEW_PLAN.md`
§0，报告自己点名的 §10 阶段 3 漏改。这**改变了该发现的状态判定**（完全修复
→ 部分修复），属实质性更正，因此保持原文不动，详见
[补充说明](REVIEW_REVIEW_PLAN_SUPPLEMENT_2026_08_08.md)；`REVIEW_PLAN.md`
v1.2 已补上漏改。
